thorntail:
  http:
    port: 8081  
  deployment:
    example-keycloak-1.0.war:
      web:
        login-config:
          auth-method: KEYCLOAK
        security-constraints:
          - url-pattern: /*
            methods: [GET]