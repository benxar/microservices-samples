thorntail:
  http:
    port: 8081
  deployment:
    thorntail-example-1.0.0.war:
      web:
        login-config:
          auth-method: KEYCLOAK
        security-constraints:
          - url-pattern: /*
            methods: [GET]